{
  "name": "o-is",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build:src": "for d in packages/*; do babel $PWD/$d/src -d $PWD/$d/lib; done ",
    "build:test": "for d in packages/*; do babel $PWD/$d/test -d $PWD/$d/.test; done",
		"build": "npm run build:src && npm run build:test",
    "test": "mocha packages/*/.test/**/*.js"
  },
  "dependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.6.0",
    "eslint": "^2.10.2",
    "lerna": "^1.1.2",
    "mocha": "^3.1.2"
  },
  "author": "",
  "license": "ISC"
}
